<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.slloan.dao.spousesOfBorrowersDao">
	<resultMap type="com.slloan.entity.SpousesOfBorrowers" id="spousesofb">
		<result column="name" property="name" />
		<result column="id_Type" property="id_Type" />
		<result column="id_Number" property="id_Number" />
		<result column="uni_Name" property="uni_Name" />
		<result column="unit_Phone" property="unit_Phone" />
		<result column="home_Phone" property="home_Phone" />
		<result column="mobile_Phone" property="mobile_Phone" />
		<result column="monthly_Income" property="monthly_Income" />
		<result column="start" property="start" />
		<result column="ctime" property="ctime" />
	</resultMap>

	<insert id="save" parameterType="com.slloan.entity.PersonalProfile">  
        insert into borrower_spouse(
     name,					
	 id_Type,       			
	 id_Number,					
	 uni_Name,						
	 unit_Phone,				
	 home_Phone,			
	 mobile_Phone,					
	 monthly_Income,			
	 start,
	 ctime) values(#{name},							
	 #{id_Type},          			
	 #{id_Number},					
	 #{uni_Name},						
	 #{unit_Phone},					
	 #{home_Phone},					
	 #{mobile_Phone},					
	 #{monthly_Income},			
	 #{start},
	 #{ctime})
    </insert>  
      <!-- 
    <update id="update" parameterType="spousesofb">    
        update t_user set user_name=#{userName},user_age=#{age} where user_id=#{id}    
    </update>   
      
    <delete id="delete" parameterType="spousesofb">    
        delete from t_user where user_id=#{id}    
    </delete>    
        
     mybsits_config中配置的alias类别名,也可直接配置resultType为类路径      
    <select id="findById" parameterType="int" resultType="spousesofb">    
        select user_id id,user_name userName,user_age age from t_user where user_id=#{id}    
    </select>    
        
    <select id="findAll" resultType="spousesofb">    
        select user_id id,user_name userName,user_age age from t_user    
    </select>   -->
	
</mapper>